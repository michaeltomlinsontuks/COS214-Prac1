#include "Canvas.h"

//Test canvas generation

int main() {
    Canvas canvas;
    // The constructor already calls generateCanvas, so this will print the blank canvas
    // If you want to call it explicitly again, uncomment the next line:
    // canvas.generateCanvas();
    return 0;
}
